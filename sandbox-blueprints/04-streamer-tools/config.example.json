{
  "_read_me": "Copy me to config.json, fill in your token, and NEVER commit config.json. It's in .gitignore at the repo root for a reason. Token = house keys.",
  "_gitignore_reminder": "If your copy of this repo doesn't ignore config.json yet, add it: echo 'config.json' >> .gitignore",
  "source": "streamlabs",
  "streamlabs_token": "YOUR_SOCKET_TOKEN",
  "twitch_channel": "your_login_lowercase",
  "dwell_ms": 4000,
  "name_cap": 24,
  "msg_cap": 120
}
